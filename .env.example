POSTGRES_DB=rental_platform
POSTGRES_USER=rental_user
POSTGRES_PASSWORD=rental_password

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123
MINIO_BUCKET=rental-files

JWT_SECRET=change_this_in_production_very_long_secret_key_12345

NODE_ENV=production

ADMIN_EMAIL=admin@rental.ma
ADMIN_PASSWORD=Admin123!

# --- Production deployment only (docker-compose.prod.yml on the VPS) ---
# GitHub username/org that owns the ghcr.io packages, in lowercase
IMAGE_OWNER=your_github_username
# Image tag to run — the CI/CD pipeline pushes "latest" and "<commit-sha>"
IMAGE_TAG=latest
